export const environment = {
  production: true,
  // In production, we use a relative path. The NGINX reverse proxy will route
  // requests starting with /api to the backend service.
  apiURL: ''
};
